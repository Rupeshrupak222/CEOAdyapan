# =========================================================================
# ADYAPAN NEXUS BACKEND GATEWAY CONFIGURATION
# =========================================================================

# Runtime
NODE_ENV=production
PORT=5000

# PostgreSQL (Prisma ORM - Supabase / Neon / Render Postgres)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# JWT Security
JWT_SECRET=adyapan_nexus_super_secure_jwt_token_secret_key_2026_enterprise

# Redis Cache (Optional - if using Redis cluster)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
